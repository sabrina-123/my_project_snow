const ShopNow={getUser(){try{return JSON.parse(localStorage.getItem('shopnow_user'))||null}catch{return null}},setUser(u){localStorage.setItem('shopnow_user',JSON.stringify(u))},logout(){localStorage.removeItem('shopnow_user');location.href='/'},isAuthenticated(){return!!this.getUser()},requireAuth(){if(!this.isAuthenticated()){location.href='/login.html?redirect='+encodeURIComponent(location.pathname);return false}return true},getCart(){try{return JSON.parse(localStorage.getItem('shopnow_cart'))||[]}catch{return[]}},saveCart(c){localStorage.setItem('shopnow_cart',JSON.stringify(c))},cartCount(){return this.getCart().reduce((s,i)=>s+i.quantity,0)},formatPrice(v){return new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR'}).format(v)},addToCart(p){if(!this.isAuthenticated()){location.href='/login.html?redirect='+encodeURIComponent(location.pathname);return}const c=this.getCart(),x=c.find(i=>i.id===p.id);if(x)x.quantity++;else c.push({...p,quantity:1});this.saveCart(c);this.refreshHeader();alert('Produit ajouté au panier.')},removeFromCart(id){this.saveCart(this.getCart().filter(i=>i.id!==id));this.refreshHeader();if(window.renderCart)window.renderCart()},updateQuantity(id,d){const c=this.getCart(),x=c.find(i=>i.id===id);if(!x)return;x.quantity+=d;this.saveCart(x.quantity<=0?c.filter(i=>i.id!==id):c);this.refreshHeader();if(window.renderCart)window.renderCart()},async api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Erreur');return d},refreshHeader(){const a=document.querySelector('[data-testid="auth-area"]');if(!a)return;const u=this.getUser();a.innerHTML=u?`<span class="user-name" data-testid="logged-user">Bonjour ${u.firstName}</span><button class="btn btn-danger" data-testid="logout-button" onclick="ShopNow.logout()">Déconnexion</button>`:`<a class="btn btn-secondary" data-testid="login-link" href="/login.html">Connexion</a><a class="btn btn-primary" data-testid="register-link" href="/register.html">Créer un compte</a>`;const c=document.querySelector('[data-testid="cart-count"]');if(c)c.textContent=this.cartCount()}};document.addEventListener('DOMContentLoaded',()=>ShopNow.refreshHeader());